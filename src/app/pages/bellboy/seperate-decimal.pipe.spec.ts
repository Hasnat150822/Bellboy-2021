import { SeperateDecimalPipe } from './seperate-decimal.pipe';

describe('SeperateDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new SeperateDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
