import { SetqueryurlDirective } from './setqueryurl.directive';

describe('SetqueryurlDirective', () => {
  it('should create an instance', () => {
    const directive = new SetqueryurlDirective();
    expect(directive).toBeTruthy();
  });
});
