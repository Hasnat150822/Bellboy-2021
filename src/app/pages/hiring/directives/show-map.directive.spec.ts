import { ShowMapDirective } from './show-map.directive';

describe('ShowMapDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMapDirective();
    expect(directive).toBeTruthy();
  });
});
