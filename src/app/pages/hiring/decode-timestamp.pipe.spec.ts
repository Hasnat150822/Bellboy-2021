import { DecodeTimestampPipe } from './decode-timestamp.pipe';

describe('DecodeTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
