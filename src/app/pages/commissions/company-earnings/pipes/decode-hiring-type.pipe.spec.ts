import { DecodeHiringTypePipe } from './decode-hiring-type.pipe';

describe('DecodeHiringTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeHiringTypePipe();
    expect(pipe).toBeTruthy();
  });
});
