import { ShowDashesPipe } from './show-dashes.pipe';

describe('ShowDashesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowDashesPipe();
    expect(pipe).toBeTruthy();
  });
});
